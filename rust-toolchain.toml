# MSRV pin (ADR-002 §MSRV): the toolchain current at Phase 2 engagement.
# Do not chase stable; review on minor bumps of clap/tracing/serde/rusqlite.
[toolchain]
channel = "1.96.1"
components = ["rustfmt", "clippy"]
